/**
 * 游戏物体基类接口
 */
interface Obj {
    /* init(): void;
    start(): void;
    update(delta: number): void;
    //draw(brush: Brush): void;
    //drawFunc: (brush: Brush) => void;
    leave(): void; */
}